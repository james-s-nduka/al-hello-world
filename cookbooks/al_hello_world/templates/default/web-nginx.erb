events {
  worker_connections 768;
  # multi_accept on;
}

http {
    server {
        listen       <%= node['al_hello_world']['web_port'] %>;
        server_name  localhost;

        location / {
            index  index.html index.htm;
            root /usr/share/nginx/html;
        }

    }
}